{
  "name": "zao-backend",
  "version": "1.0.0",
  "private": true,
  "description": "PC backend for ZAO - wraps llama-server (Qwen2.5-Coder-3B) with a small HTTP layer the app connects to over LAN or a Cloudflare Quick Tunnel, plus a Playwright-driven browser agent streamed live to the phone.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0",
    "playwright": "^1.47.0"
  }
}
